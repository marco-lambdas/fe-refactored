import styled from 'styled-components';

const CollectionsHeaderWrapper = styled.div`
  .collection-header-main-container {
    margin-top: 10rem;
  }

  .collection-header {
    text-align: center;
  }

  .collection-header-text-small {
    color: #a239ca !important;
  }

  .collection-header-text {
    font-family: 'Varela Round', serif;
    font-weight: 700 !important;
    letter-spacing: 2px;
    color: #0e0b16;
  }
`;

export default CollectionsHeaderWrapper;
